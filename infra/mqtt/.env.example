# Copia este archivo a .env (gitignored) y pon un valor real.
# Debe COINCIDIR con la contraseña del usuario 'sidru' creado con mosquitto_passwd,
# y con sidru.mqtt.password del backend (env MQTT_PASSWORD).
MQTT_PASSWORD=cambia-esto-en-local
